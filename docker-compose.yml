services:
  api:
    build:
      context: ./services/api
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"

  web:
    build:
      context: ./services/web
    ports:
      - "3000:80"
    depends_on:
      - api
